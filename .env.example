# ──────────────────────────────────────────
# Copy this file to .env and fill in values
# ──────────────────────────────────────────

# Database (get a free URL at neon.tech)
DATABASE_URL=postgresql://user:pass@host/titra

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_REFRESH_SECRET=change-me-to-another-long-random-string

# Next.js
NEXTAUTH_SECRET=change-me-to-a-long-random-string
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (optional — leave blank to disable)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Email — get a free API key at resend.com
RESEND_API_KEY=
# Sender address — must be from a verified domain in your Resend account.
# For quick testing use: onboarding@resend.dev (can only send to your own Resend account email)
EMAIL_FROM=onboarding@resend.dev

# File uploads — get free credentials at cloudinary.com
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# App URLs
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_APP_URL=http://localhost:3000
